services:
  - type: web
    name: unstop-mcp
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py --transport sse --host 0.0.0.0 --port $PORT
    healthCheckPath: /sse
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
